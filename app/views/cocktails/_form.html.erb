<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name, label: "Cocktail name" %>
  <!-- Button to append ingredient + dose => dose form appears without reloading page + delete button -->
  <!-- <%# render 'doses/new' %> -->
  <%= f.button :submit, "Submit", class: "btn btn-success" %>
<% end %>
